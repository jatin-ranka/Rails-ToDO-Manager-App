<h1>This is todos list</h1>

<h2>Todos: <%= Todo.count%> </h2>

<%= form_with url: "/todos", method: :post do%>
  <input type="text" name="todo_text" placeholder="Type todo here...">
  <input type="date" name="due_date">
  <button type="submit" class="primary-btn"> Add </button>
<% end %>

<%= render partial: "todos", locals: {name: "Over Due", todos: @todos.over_due} %>
<%= render partial: "todos", locals: {name: "Due today", todos: @todos.due_today} %>
<%= render partial: "todos", locals: {name: "Due Later", todos: @todos.due_later} %>
